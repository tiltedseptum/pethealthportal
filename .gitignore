# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
env/

# Real medical-record artifacts generated by running the pipeline locally --
# these contain a real pet's health data and must never be committed.
latest_visit.pdf
*.pdf
parsed_output.md

# Stray backup/dump file (leftover copy of health_agent.py, not real source)
health_agent.txt
